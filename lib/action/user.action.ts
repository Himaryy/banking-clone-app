"use server";

export const signIn = async () => {
  try {
    // Mutation / database / make fetch
  } catch (error) {
    console.log(error);
  }
};

export const signUp = async () => {
  try {
    // Mutation / database / make fetch
  } catch (error) {
    console.log(error);
  }
};
